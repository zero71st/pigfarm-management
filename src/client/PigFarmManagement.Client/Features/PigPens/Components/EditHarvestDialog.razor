@using Microsoft.AspNetCore.Components
@using MudBlazor
@using PigFarmManagement.Shared.Models
@using PigFarmManagement.Client.Features.PigPens.Services
@inject IPigPenService PigPenService
@inject ISnackbar Snackbar

<MudDialog>
    <DialogContent>
        <MudGrid Spacing="3">
            <!-- Pig Tracking Info Card -->
            @if (PigPen != null && ExistingHarvests != null)
            {
                <MudItem xs="12">
                    <MudCard Style="background-color: #f8f9fa;">
                        <MudCardContent>
                            <MudGrid>
                                <MudItem xs="12">
                                    <MudText Typo="Typo.subtitle2" Class="mb-2">
                                        🐷 ข้อมูลคอกสุกร
                                    </MudText>
                                </MudItem>
                                <MudItem xs="6">
                                    <MudText Typo="Typo.body2" Class="text-muted">
                                        จำนวนสุกรทั้งหมด:
                                    </MudText>
                                    <MudText Typo="Typo.body1" Style="font-weight: 600;">
                                        @PigPen.PigQty ตัว
                                    </MudText>
                                </MudItem>
                                <MudItem xs="6">
                                    <MudText Typo="Typo.body2" Class="text-muted">
                                        จับแล้ว (รายการอื่น):
                                    </MudText>
                                    <MudText Typo="Typo.body1" Style="font-weight: 600;">
                                        @otherHarvestedPigs ตัว
                                    </MudText>
                                    <MudText Typo="Typo.caption" Class="text-muted">
                                        (ไม่รวมรายการนี้)
                                    </MudText>
                                </MudItem>
                                
                                @if (availablePigs > 0)
                                {
                                    <MudItem xs="12">
                                        <MudAlert Severity="Severity.Info" Dense="true">
                                            <MudText Typo="Typo.body2">
                                                <strong>@availablePigs ตัว</strong> พร้อมสำหรับรายการนี้ (รวมจำนวนปัจจุบัน)
                                            </MudText>
                                        </MudAlert>
                                    </MudItem>
                                }
                                else if (totalPigs > availablePigs)
                                {
                                    <MudItem xs="12">
                                        <MudAlert Severity="Severity.Warning" Dense="true">
                                            <MudText Typo="Typo.body2">
                                                <strong>คำเตือน:</strong> จำนวนปัจจุบัน (@totalPigs) เกินจำนวนที่คอกรองรับ
                                            </MudText>
                                        </MudAlert>
                                    </MudItem>
                                }
                            </MudGrid>
                        </MudCardContent>
                    </MudCard>
                </MudItem>
            }

                <MudItem xs="12">
                    <MudForm @ref="form" @bind-IsValid="@isFormValid">
                        <MudGrid>
                            <!-- Harvest Date -->
                            <MudItem xs="12" md="6">
                                <MudDatePicker @bind-Date="harvestDate"
                                               Label="วันที่จับสุกร"
                                               Required="true"
                                               RequiredError="กรุณาระบุวันที่จับสุกร"
                                               MaxDate="DateTime.Today"
                                               HelperText="วันที่ทำการจับสุกร"
                                               Variant="Variant.Outlined" />
                            </MudItem>

                            <!-- Total Pigs -->
                            <MudItem xs="12" md="6">
                                <MudNumericField @bind-Value="totalPigs"
                                                 Label="จำนวนสุกร"
                                                 Required="true"
                                                 RequiredError="กรุณาระบุจำนวนสุกร"
                                                 Min="1"
                                                 Max="@(availablePigs > 0 ? availablePigs : PigPen?.PigQty ?? 1000)"
                                                 HelperText="จำนวนสุกรที่จับ"
                                                 Variant="Variant.Outlined"
                                                 Adornment="Adornment.End"
                                                 AdornmentText="ตัว"
                                                 OnBlur="CalculateRevenue" />
                            </MudItem>

                            <!-- Price Per Kg -->
                            <MudItem xs="12" md="6">
                                <MudNumericField @bind-Value="pricePerKg"
                                                 Label="ราคาต่อกิโลกรัม"
                                                 Required="true"
                                                 RequiredError="กรุณาระบุราคาต่อกิโลกรัม"
                                                 Min="0.01m"
                                                 Step="0.01m"
                                                 Format="F2"
                                                 HelperText="ราคาขายต่อกิโลกรัม"
                                                 Variant="Variant.Outlined"
                                                 Adornment="Adornment.Start"
                                                 AdornmentText="฿"
                                                 OnBlur="CalculateRevenue" />
                            </MudItem>

                            <!-- Total Weight -->
                            <MudItem xs="12" md="6">
                                <MudNumericField @bind-Value="totalWeight"
                                                 Label="น้ำหนักรวม"
                                                 Required="true"
                                                 RequiredError="กรุณาระบุน้ำหนักรวม"
                                                 Min="0.1m"
                                                 Step="0.1m"
                                                 Format="F1"
                                                 HelperText="น้ำหนักรวมของสุกรทั้งหมด"
                                                 Variant="Variant.Outlined"
                                                 Adornment="Adornment.End"
                                                 AdornmentText="กก."
                                                 OnBlur="CalculateWeightMetrics" />
                            </MudItem>

                            <!-- Calculated Metrics Display -->
                            @if (totalPigs > 0 && totalWeight > 0)
                            {
                                <MudItem xs="12">
                                    <MudDivider Class="my-2" />
                                    <MudText Typo="Typo.subtitle2" Class="mb-2">
                                        📊 ข้อมูลคำนวณ
                                    </MudText>
                                </MudItem>

                                <MudItem xs="12" md="4">
                                    <MudPaper Class="pa-3" Elevation="1">
                                        <MudText Typo="Typo.body2" Color="Color.Secondary">น้ำหนักเฉลี่ย</MudText>
                                        <MudText Typo="Typo.h6">@averageWeight.ToString("F1") กก.</MudText>
                                    </MudPaper>
                                </MudItem>

                                <MudItem xs="12" md="4">
                                    <MudPaper Class="pa-3" Elevation="1">
                                        <MudText Typo="Typo.body2" Color="Color.Secondary">รายได้ต่อตัว</MudText>
                                        <MudText Typo="Typo.h6" Color="Color.Success">฿@revenuePerPig.ToString("N0")</MudText>
                                    </MudPaper>
                                </MudItem>

                                <MudItem xs="12" md="4">
                                    <MudPaper Class="pa-3" Elevation="1">
                                        <MudText Typo="Typo.body2" Color="Color.Secondary">รายได้รวม</MudText>
                                        <MudText Typo="Typo.h6" Color="Color.Success">฿@totalRevenue.ToString("N0")</MudText>
                                    </MudPaper>
                                </MudItem>
                            }

                        </MudGrid>
                    </MudForm>
                </MudItem>
            </MudGrid>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">ยกเลิก</MudButton>
        <MudButton Color="Color.Primary" 
                   Variant="Variant.Filled" 
                   OnClick="SaveHarvest" 
                   Disabled="@(!isFormValid || isSaving)"
                   StartIcon="@(isSaving ? null : Icons.Material.Filled.Save)">
            @if (isSaving)
            {
                <MudProgressCircular Class="ms-n1" Size="Size.Small" Indeterminate="true" />
                <MudText Class="ms-2">กำลังบันทึก...</MudText>
            }
            else
            {
                <MudText>บันทึก</MudText>
            }
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; } = null!;

    [Parameter] public HarvestResult HarvestToEdit { get; set; } = new HarvestResult(Guid.Empty, Guid.Empty, DateTime.Today, 0, 0, 0, 0, 0);
    [Parameter] public PigPen? PigPen { get; set; }
    [Parameter] public List<HarvestResult>? ExistingHarvests { get; set; }

    private MudForm form = null!;
    private bool isFormValid;
    private bool isSaving = false;

    // Form fields (in the order requested by user)
    private DateTime? harvestDate = DateTime.Today;
    private int totalPigs = 0;
    private decimal pricePerKg = 0;
    private decimal totalWeight = 0;

    // Calculated values
    private decimal averageWeight => totalPigs > 0 ? totalWeight / totalPigs : 0;
    private decimal totalRevenue => totalWeight * pricePerKg;
    private decimal revenuePerPig => totalPigs > 0 ? totalRevenue / totalPigs : 0;

    // Pig tracking (excluding the current harvest being edited)
    private int otherHarvestedPigs => ExistingHarvests?.Where(h => h.Id != HarvestToEdit.Id).Sum(h => h.PigCount) ?? 0;
    private int availablePigs => PigPen?.PigQty - otherHarvestedPigs ?? 0;

    protected override void OnInitialized()
    {
        // Pre-populate form with existing harvest data
        if (HarvestToEdit != null)
        {
            harvestDate = HarvestToEdit.HarvestDate;
            totalPigs = HarvestToEdit.PigCount;
            pricePerKg = HarvestToEdit.SalePricePerKg;
            totalWeight = HarvestToEdit.TotalWeight;
        }
    }

    private void CalculateRevenue()
    {
        StateHasChanged();
    }

    private void CalculateWeightMetrics()
    {
        // Weight metrics are now calculated automatically from total weight and pig count
        StateHasChanged();
    }

    private async Task SaveHarvest()
    {
        if (!isFormValid || isSaving) return;

        isSaving = true;
        StateHasChanged();

        try
        {
            // Validate the form
            await form.Validate();
            if (!form.IsValid) 
            {
                isSaving = false;
                return;
            }

            // Create the updated harvest DTO
            var updateDto = new HarvestUpdateDto(
                harvestDate!.Value,
                totalPigs,
                averageWeight,
                totalWeight,
                pricePerKg,
                totalRevenue
            );

            // Call the API to update the harvest
            var result = await PigPenService.UpdateHarvestResultAsync(HarvestToEdit.PigPenId, HarvestToEdit.Id, updateDto);
            
            Snackbar.Add($"Harvest record updated successfully. Revenue: ฿{totalRevenue:N2}", Severity.Success);
            MudDialog.Close(DialogResult.Ok(result));
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error updating harvest: {ex.Message}", Severity.Error);
        }
        finally
        {
            isSaving = false;
            StateHasChanged();
        }
    }

    private void Cancel()
    {
        MudDialog.Cancel();
    }
}